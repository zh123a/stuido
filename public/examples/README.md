# 示例成片

本目录存放 W3 本地验证生成的示例视频（git 追踪，供直接预览）。

*   `demo_高铁硬币_12分镜_7MG_72s.mp4` 195K 1920x1080 72.2s 12分镜7MG + BGM混音 438K + MG PNG 7张
*   `demo_W3验证_6分镜_3MG_36s.mp4` 75K 1920x1080 36s 6分镜3MG

生成于 `apps/web/renders/*/final_with_audio.mp4`，可直接 `open public/examples/*.mp4` 预览。

完整产物含 `mg_*.png` `bgm.m4a` `subs.srt` `scene*.mp4` 见 `apps/web/renders/<projectId>/`。
